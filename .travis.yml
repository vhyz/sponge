language: cpp
sudo: required
dist: trusty
compiler:
  - gcc
os:
  - linux
before-install:
  - sudo apt-get update
addons:
  apt:
    source:
      - ubuntu-toolchain-r-test
    packages:
      - g++-7
script:
  - mkdir build
  - cd build
  - cmake ..
  - make -j16